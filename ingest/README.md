# ingest

Kite client, yfinance fallback, NSE bhavcopy, GDELT/news, macro fetchers, and token-refresh cron.
`upstox_expired` provides a token-driven, resumable expired-NIFTY-futures minute-bar backfill.
